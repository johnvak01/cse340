<%- include('partials/header') %>
    <main>
        <h1>
            <%= title %>
        </h1>
        <h2>Categories we assist with!</h2>
        <ul>
            <% categories.forEach(category => { %>
                <a href="/category/<%= category.category_id%>"><li><%= category.category_name %></li></a>
            <% }); %>
        </ul>
    </main>
    <%- include('partials/footer') %>